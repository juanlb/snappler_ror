<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
<%= devise_error_messages! %>
 <fieldset class="quarter center-screen">
    <h4>Registrarse</h4>
    <p>
      <%= f.label :email, "E-mail" %>
      <%= f.email_field :email %>
    </p>
    <p>
      <%= f.label :password, "Contraseña" %>
      <%= f.password_field :password %>
    </p>
    <p>
      <%= f.label :password_confirmation, "Confirmacion Contraseña" %>
      <%= f.password_field :password_confirmation %>
    </p>
    <p class="button-content">
      <%= f.submit "Registrarse", :style=>"margin-left: 110px;", :class=>"link-boton boton-graystrong without-image" %>
    </p>
  </fieldset>

<% end %>
<%=  link_to 'Ingresar', "/users/sign_in" %>
